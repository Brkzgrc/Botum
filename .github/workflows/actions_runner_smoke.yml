name: actions runner smoke check
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/actions_runner_smoke.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  smoke:
    runs-on: ubuntu-24.04
    steps:
      - name: Verify runner
        run: echo "runner-ok"
